<mvc:View
	controllerName="app.controller.Category"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc">
	<Page
		title="{i18n>homePageTitle}">
		<List
			id="category"
			headerText="{i18n>categoryListTitle}"
			class="sapUiResponsiveMargin"
			width="auto"
			items="{
					path : 'invoice>/Invoices',
					sorter : {
						path : 'ProductName'
					}
				}">
			<headerToolbar>
					<Toolbar>
						<Title text="{i18n>categoryListTitle}"/>
						<ToolbarSpacer/>
						<SearchField width="50%" search=".onFilterInvoices"/>
					</Toolbar>
				</headerToolbar>
			<items>
				<ObjectListItem
					icon="{invoice>icon}"
					iconDensityAware="false"
					title="{invoice>ProductName}"
					number="{
						path: 'invoice>types'
					}"
					type="Navigation"
					press="onPress"
				/>
			</items>
		</List>
	</Page>
</mvc:View>
